const fullname = "Amandeep Singh";
const age = 33;
const hobbies = ["Cricket", "Football", "Video Games", "Chess"];

const str = `My name is ${fullname} and age is ${age}. My hobies are ${hobbies.join(
  ", "
)}.`;

console.log(str);
